server:
  port: ${PORT:8080}

spring:
  application:
    name: api-gateway

  autoconfigure:
    exclude:                                                        # ← ADD THIS
      - org.springframework.boot.autoconfigure.security.reactive.ReactiveSecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.reactive.ReactiveUserDetailsServiceAutoConfiguration

  cloud:
    gateway:
      # ❌ REMOVE default-filters DedupeResponseHeader block
      # ❌ REMOVE globalcors block entirely
      # ✅ Keep only routes below

      routes:
        - id: auth-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/auth/**

        - id: doctor-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/doctors/**

        - id: admin-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/admin/**

        - id: slots-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/slots/**

        - id: pharmacy-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/pharmacy/**

        - id: patient-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/patients/**

        - id: medical-records-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/records/**

        - id: appointment-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/appointments/**

        - id: billing-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/billing/**

        - id: payments-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/payments/**

        - id: notifications-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/notifications/**
          filters:
            - RemoveResponseHeader=Transfer-Encoding

        - id: queue-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/queue/**

        - id: reports-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/reports/**

        - id: ratings-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/ratings/**

        - id: public-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/public/**

        - id: uploads-service
          uri: https://medvault-backend-production.up.railway.app
          predicates:
            - Path=/uploads/**
          filters:
            - AddResponseHeader=Cache-Control, max-age=3600

        - id: chat-service
          uri: wss://medvault-backend-production.up.railway.app
          predicates:
            - Path=/api/chat/**

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://eureka-server:8761/eureka/}
    fetch-registry: true
    register-with-eureka: true
    registry-fetch-interval-seconds: 30
    eureka-server-connect-timeout-seconds: 10
    eureka-server-read-timeout-seconds: 10
  instance:
    prefer-ip-address: false
    hostname: medvault-gateway
    non-secure-port: 8080
    instance-id: ${spring.application.name}:${server.port}
    health-check-url-path: /actuator/health
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90

jwt:
  secret: ${JWT_SECRET:medvault_super_secret_key_2024_should_be_very_long_for_security}
  expiration: 86400000

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - gateway
  endpoint:
    gateway:
      enabled: true
    health:
      show-details: always

logging:
  level:
    org.springframework.cloud.gateway: INFO
    org.springframework.security: WARN
    com.eureka_Gateway: INFO
    reactor.netty: WARN